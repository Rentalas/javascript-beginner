// @ts-ignore
const addNumbers = (num: number, ...arg: number[]): number => {
};

// @ts-ignore
const sumArray = (arr: number[]): number => {   
}

// @ts-ignore
const multiplyAll = (arr: Array<number[]>): number => {};


// @ts-ignore
const camelize = (str: string): string => {};

// @ts-ignore
const filterRange = (numbers: number[], from: number, end: number): number[] => {};

export type Human = {name: string, age: number};

// @ts-ignore
const nameMapping = (gamers: Human[]): string[] => {};
    